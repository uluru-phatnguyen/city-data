import React from "react";
import { Container } from "@components";
// import { createColumnHelper } from "@tanstack/react-table";

const Home: React.FC = () => {
  return <Container>City Data Dashboard</Container>;
};

export default Home;
